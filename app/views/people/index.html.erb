<h1>People</h1>
<h4>New Person</h4>
 <%= render 'form', person: @person %> 
<br>
<h4>Existing People</h4>

<div class="form-row align-items-end">
    <div class="form-group col-md-4">
    <b>Name</b>
    </div>
    <div class="form-group col-md-4">
    <b>Kind</b>
    </div>
</div>

<% Person.order(created_at: :desc).each do |person| %>
  <%= render 'edit_form', person: person %>
<% end %>
