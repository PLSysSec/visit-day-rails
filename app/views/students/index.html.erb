<h1>Students</h1>
<h4>New Student</h4>
 <%= render 'form', student: @student %> 
<br>
<h4>Existing Students</h4>

<div class="form-row align-items-end">
    <div class="form-group col-md-4">
    <b>Name</b>
    </div>
    <div class="form-group col-md-4">
    <b>Group</b>
    </div>
</div>
  <% Student.order(created_at: :desc).each do |student| %>
    <%= render 'edit_form', student: student %>
  <% end %>
